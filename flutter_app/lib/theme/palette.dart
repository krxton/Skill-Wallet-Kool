import 'package:flutter/material.dart';

/// ปรับสีให้ตรง Figma ที่นี่จุดเดียว
class Palette {
  // Base
  static const cream = Color(0xFFFFF5CD);
  static const text = Colors.black87;

  // Brand / Primary
  static const sky = Color(0xFF0D92F4); // ชื่อเรื่องสีฟ้า
  static const blueChip = Color(0xFF59B3FF); // ปุ่ม/กรอบฟ้าอ่อน
  static const blueBtn = Color(0xFF6EC1FF); // PLAY SECTION

  // Accents ตาม Figma
  static const purple = Color(0xFFB67CFF); // CAST TO TV
  static const yellow = Color(0xFFFFD45E); // AIRPLAY
  static const green = Color(0xFF66BB6A); // OK/NEXT/Finish
  static const red = Color(0xFFE85C5C); // RECORD/label แดง
  static const greyCard = Color(0xFFE9E9EB); // การ์ดเทา/placeholder
  static const divider = Color(0xFFE5E5E5);

  // Progress bars
  static const progressBg = Color(0xFFEEE8D5);
  static const progressFill = Color(0xFF8ED081);
}
